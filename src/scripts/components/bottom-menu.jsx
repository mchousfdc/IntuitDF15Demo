var React = require('react/addons');

require('bottom-menu.css');

module.exports = React.createClass({
    render: function() {
        return (
            <div>
                <p>This is a bottom-menu component</p>
            </div>
        );
    }
});
