var React = require('react/addons');

require('../../styles/invoices.css');

module.exports = React.createClass({
    render: function () {
        return (
            <div>
                <p>This is a invoices component</p>
            </div>
        );
    }
});
